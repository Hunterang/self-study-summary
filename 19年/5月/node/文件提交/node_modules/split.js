

exports.uilt = function (buffer,key) {
  let arr = []
  let n = 0
  while ((n = buffer.indexOf(key))!== -1) {
    arr.push(buffer.slice(0,n))
    buffer = buffer.slice(n+key.length)
  }
  arr.push(buffer)

  return arr
}
